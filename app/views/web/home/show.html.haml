#index-banner.section.no-pad-bot
  .container
    %h1.header.center
      1 vs 100
      %br
      %br

    %h5.center
      = render partial: "web/home/roles/#{@new_cheering.employee_role}"
      = render partial: 'form', locals: { user_cheering: @new_cheering, url: user_cheerings_path }
